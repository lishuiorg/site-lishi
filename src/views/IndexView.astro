---
/* 索引页：模板取自底座，本站按年代分期分组，见 site/model.mjs。 */
import IndexView from 'lishui-kit/astro/views/IndexView.astro';
import { indexModel } from '../site/model.mjs';

const { lang } = Astro.props;
---

<IndexView {...indexModel(lang)} />
