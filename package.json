{
  "name": "site-lishi",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "溧水历史站点库。用 Astro 读取 lishui-history 内容库，生成静态站点，部署到 lishi.lishui.org。",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "validate": "node ../lishui-history/scripts/validate.mjs",
    "check-links": "node scripts/check-links.mjs",
    "check-pages": "node scripts/check-pages.mjs",
    "check": "npm run validate && npm run build && npm run check-links && npm run check-pages"
  },
  "dependencies": {
    "lishui-kit": "file:../lishui-kit"
  },
  "devDependencies": {
    "astro": "^5.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
